#include "main.h"
/**
 * _abs - get the absolute value of intger
 * @n: number to get value from
 * Return: absolute value of number
 */
int _abs(int n)
{
	if (n < 0)
		return (-1 * n);
	return (n);
}
