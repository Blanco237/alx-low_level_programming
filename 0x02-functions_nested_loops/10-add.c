#include "main.h"
/**
 * add - a function that adds 2 integers and returns the result
 * @a: first number input
 * @b: second number input
 * Return: one + two
 */
int add(int a, int b)
{
	return (a + b);
}
