#include "main.h"
#include <stdio.h>
/**
 * _isupper - checks for uppercase character
 * @c: character to check
 * Return: 1 if c is upper, 0 otherwise
 */
int _isupper(int c)
{
	return (isupper(c));
}
